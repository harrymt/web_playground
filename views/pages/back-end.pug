extends ../layout

block content
  h1= title
  p Welcome to #{title}

  h2(id='server') Server
  p Details about how the server was constructed.

  h2(id='database') Database
  p Details about how the database works.

  h2(id='dynamic') Dynamic Pages
  p Details about how dynamic pages are displayed. Pug.
